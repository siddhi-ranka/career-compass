// Firebase removed: stub exports so the app does not require the Firebase SDK.
// If you later want auth again, replace this file with your Firebase initialization.

export const auth = null;
export const db = null;
export default null;
